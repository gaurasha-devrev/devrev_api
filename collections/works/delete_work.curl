#!/bin/bash
# Delete a work item

curl -X POST "https://{{base_url}}/works.delete" \
  -H "Authorization: Bearer {{DEVREV_TOKEN}}" \
  -H "Content-Type: application/json" \
  -d '{
    "id": "{{work_id}}"
  }'
