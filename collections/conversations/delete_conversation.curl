#!/bin/bash
# Delete a conversation

curl -X POST "https://{{base_url}}/conversations.delete" \
  -H "Authorization: Bearer {{DEVREV_TOKEN}}" \
  -H "Content-Type: application/json" \
  -d '{
    "id": "{{conversation_id}}"
  }'
