#!/bin/bash
# List conversations with optional filters

curl -X GET "https://{{base_url}}/conversations.list?limit=20" \
  -H "Authorization: Bearer {{DEVREV_TOKEN}}"

# With filtering (uncomment and modify as needed):
# curl -X GET "https://{{base_url}}/conversations.list?limit=20&applies_to_part={{part_id}}" \
#   -H "Authorization: Bearer {{DEVREV_TOKEN}}"

# Filter by member:
# curl -X GET "https://{{base_url}}/conversations.list?limit=20&members={{rev_user_id}}" \
#   -H "Authorization: Bearer {{DEVREV_TOKEN}}"
