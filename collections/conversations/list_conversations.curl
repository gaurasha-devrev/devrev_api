#!/bin/bash
# List conversations with optional filters

curl -X GET "https://api.devrev.ai/conversations.list?limit=20" \
  -H "Authorization: Bearer $DEVREV_TOKEN"

# With filtering (uncomment and modify as needed):
# curl -X GET "https://api.devrev.ai/conversations.list?limit=20&applies_to_part=$PART_ID" \
#   -H "Authorization: Bearer $DEVREV_TOKEN"

# Filter by member:
# curl -X GET "https://api.devrev.ai/conversations.list?limit=20&members=$REV_USER_ID" \
#   -H "Authorization: Bearer $DEVREV_TOKEN"
