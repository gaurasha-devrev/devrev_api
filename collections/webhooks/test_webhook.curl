#!/bin/bash
# Test webhook by sending a test event

curl -X POST "https://api.devrev.ai/webhooks.test" \
  -H "Authorization: Bearer $DEVREV_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "id": "'$WEBHOOK_ID'"
  }'
