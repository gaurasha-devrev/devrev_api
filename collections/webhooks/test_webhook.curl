#!/bin/bash
# Test webhook by sending a test event

curl -X POST "https://{{base_url}}/webhooks.test" \
  -H "Authorization: Bearer {{DEVREV_TOKEN}}" \
  -H "Content-Type: application/json" \
  -d '{
    "id": "{{webhook_id}}"
  }'
