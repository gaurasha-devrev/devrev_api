#!/bin/bash
# Delete webhook permanently

curl -X POST "https://{{base_url}}/webhooks.delete" \
  -H "Authorization: Bearer {{DEVREV_TOKEN}}" \
  -H "Content-Type: application/json" \
  -d '{
    "id": "{{webhook_id}}"
  }'
