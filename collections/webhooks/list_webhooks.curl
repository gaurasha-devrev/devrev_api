#!/bin/bash
# List all webhooks

curl -X GET "https://api.devrev.ai/webhooks.list?limit=20" \
  -H "Authorization: Bearer $DEVREV_TOKEN"

# With filtering (uncomment and modify as needed):
# curl -X GET "https://api.devrev.ai/webhooks.list?limit=20&enabled=true" \
#   -H "Authorization: Bearer $DEVREV_TOKEN"
