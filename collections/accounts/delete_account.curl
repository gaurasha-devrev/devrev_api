#!/bin/bash
# Delete an account

curl -X POST "https://{{base_url}}/accounts.delete" \
  -H "Authorization: Bearer {{DEVREV_TOKEN}}" \
  -H "Content-Type: application/json" \
  -d '{
    "id": "{{account_id}}"
  }'
