#!/bin/bash
# Delete/revoke an authentication token

curl -X POST "https://{{base_url}}/auth-tokens.delete" \
  -H "Authorization: Bearer {{DEVREV_TOKEN}}" \
  -H "Content-Type: application/json" \
  -d '{
    "id": "{{token_id}}"
  }'
