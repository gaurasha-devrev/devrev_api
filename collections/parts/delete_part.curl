#!/bin/bash
# Delete a part

curl -X POST "https://{{base_url}}/parts.delete" \
  -H "Authorization: Bearer {{DEVREV_TOKEN}}" \
  -H "Content-Type: application/json" \
  -d '{
    "id": "{{part_id}}"
  }'
