#!/bin/bash
# Update an existing part

curl -X POST "https://{{base_url}}/parts.update" \
  -H "Authorization: Bearer {{DEVREV_TOKEN}}" \
  -H "Content-Type: application/json" \
  -d '{
    "id": "{{part_id}}",
    "description": "Updated test product description"
  }'
