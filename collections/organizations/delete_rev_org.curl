#!/bin/bash
# Delete rev organization

curl -X POST "https://{{base_url}}/rev-orgs.delete" \
  -H "Authorization: Bearer {{DEVREV_TOKEN}}" \
  -H "Content-Type: application/json" \
  -d '{
    "id": "{{rev_org_id}}"
  }'
