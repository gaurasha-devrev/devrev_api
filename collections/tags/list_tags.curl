#!/bin/bash
# List all tags

curl -X GET "https://{{base_url}}/tags.list?limit=50" \
  -H "Authorization: Bearer {{DEVREV_TOKEN}}"

# With filtering (uncomment and modify as needed):
# curl -X GET "https://{{base_url}}/tags.list?limit=50&name=test" \
#   -H "Authorization: Bearer {{DEVREV_TOKEN}}"
