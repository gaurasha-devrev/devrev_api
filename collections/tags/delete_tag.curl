#!/bin/bash
# Delete a tag

curl -X POST "https://{{base_url}}/tags.delete" \
  -H "Authorization: Bearer {{DEVREV_TOKEN}}" \
  -H "Content-Type: application/json" \
  -d '{
    "id": "{{tag_id}}"
  }'
