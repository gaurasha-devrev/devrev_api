#!/bin/bash
# Delete an artifact

curl -X POST "https://api.devrev.ai/artifacts.delete" \
  -H "Authorization: Bearer $DEVREV_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "id": "'$ARTIFACT_ID'"
  }'
