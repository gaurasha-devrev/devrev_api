#!/bin/bash
# Delete an artifact

curl -X POST "https://{{base_url}}/artifacts.delete" \
  -H "Authorization: Bearer {{DEVREV_TOKEN}}" \
  -H "Content-Type: application/json" \
  -d '{
    "id": "{{artifact_id}}"
  }'
