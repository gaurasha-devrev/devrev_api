#!/bin/bash
# Delete a timeline entry

curl -X POST "https://{{base_url}}/timeline-entries.delete" \
  -H "Authorization: Bearer {{DEVREV_TOKEN}}" \
  -H "Content-Type: application/json" \
  -d '{
    "id": "{{timeline_entry_id}}"
  }'
